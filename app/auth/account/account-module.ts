module auth.account {
  "use strict";

  /**
   * @ngdoc object
   * @name auth.account
   * @description account overview (sign-up, login, logout)
   *
   **/
  angular
    .module("auth.account",
      ["ui.router"]
    );
}
