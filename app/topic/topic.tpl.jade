.topic-bg
  .l-site-width
    h1 {{::topic.title}}
    p {{::topic.language}}

    div(ui-view="")

    .ex-nav
      a.ex-nav--prev(href=""
      ng-click="topic.previousExercise()"
      ng-show="topic.hasPreviousExercise()")

      a.ex-nav--next(href=""
      ng-click="topic.nextExerci  se()"
      ng-show="topic.hasNextExercise()")

